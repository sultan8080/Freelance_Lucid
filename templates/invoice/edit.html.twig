{% extends 'dashboard/base.html.twig' %}

{% block title %}Edit Invoice
{% endblock %}

{% block dashboard_content %}
	<div class="max-w-6 mx-auto">
		<a href="{{ path('app_invoice_index') }}" class="text-slate-400 font-bold mb-6 block hover:text-primary">
			<i class="fas fa-arrow-left mr-2"></i>
			Back to list
		</a>

		<div class="glass-card rounded-bento p-8">
			<p class="text-slate-500 text-base md:text-lg mb-4 md:mb-8">
				Edit client details
			</p>


			{# The form block #}
			{% block client_form %}
				{{ include('invoice/_form.html.twig', {'button_label': 'Update'}) }}
			{% endblock %}
			<div class="mt-6">
				{{ include('invoice/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
